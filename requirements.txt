# AI Resume Screening & Skill Gap Analyzer
# Requirements optimized for Streamlit Community Cloud
# CPU-only PyTorch, no CUDA/NVIDIA dependencies

# Core dependencies
streamlit==1.38.0
pandas==2.2.3
numpy==1.26.4

# PDF and document processing
pypdf==4.2.0
python-docx==1.1.2
pdfplumber==0.11.4

# NLP and ML (CPU-only)
transformers==4.33.2
sentence-transformers==2.7.0
torch==2.12.1

# PDF generation for reports
reportlab==4.2.5

# Optional: For better text processing
nltk==3.9.1
spacy==3.8.1

# Optional: OCR for scanned PDFs (also requires Tesseract installed on system)
pytesseract==0.3.13
pdf2image==1.17.0
